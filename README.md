# budowac-client

Vite + TypeScript + ThreeJS client (skeleton): landing scene, net stubs,
duplicated proto tags.

```
npm install
npm run dev    # http://localhost:5173
```

Env (build-time via Vite): `VITE_API_URL`, `VITE_GATEWAY_URL`.
